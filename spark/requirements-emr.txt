# EMR Serverless 커스텀 이미지용 Python 의존성.
# pyspark는 베이스 이미지(public.ecr.aws/emr-serverless/spark/emr-7.2.0)가 제공하므로 제외.
# pytest/moto(테스트 전용), pydeequ(대상 잡 미사용)도 제외 - ingestion/requirements.txt 참고.
# pyiceberg도 제외 - 대상 잡이 실제로 import하는 ingestion/common 파일(s3_utils.py,
# spark_session.py, sql_assert.py)은 pyiceberg를 쓰지 않는다(iceberg_catalog.py는
# COPY는 되지만 이 4개 잡 어디서도 import하지 않음). 게다가 pyiceberg==0.11.1은
# Python>=3.10을 요구해 EMR 이미지의 Python 3.9와 애초에 호환되지 않는다(실측: pip
# install 시 "No matching distribution" - PyPI 배포 wheel이 cp310부터 시작).
boto3>=1.34
botocore>=1.34
requests>=2.31
tenacity>=8.2
pandas>=2.0
openpyxl>=3.1
duckdb>=0.10.0
pyarrow>=14.0.0

# risk_model 모델 로드/추론용 (pipeline/risk_model/jobs, pipeline/train_risk_model 참고,
# airflow/requirements.txt와 동일 버전 고정)
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3
PyYAML>=6.0
