# DAG에서 필요한 추가 Python 의존성을 여기에 추가 (예: pandas, boto3, requests).
# apache-airflow-providers-* 는 provider 패키지이므로 pip 대신 constraints 파일과 함께 설치할 것.

# pipeline/risk_model: risk_model_v1.joblib(LGBM) 로드/추론에 필요
boto3>=1.34
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3
