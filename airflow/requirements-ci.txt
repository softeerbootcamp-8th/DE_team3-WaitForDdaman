# CI 전용 - DAG 구조 테스트(DagBag 파싱)를 실행하기 위한 Airflow 본체.
#
# airflow/requirements.txt에는 넣지 않는다. 그 파일은 airflow 베이스 이미지 위에
# 설치되므로 여기서 airflow 본체를 다시 지정하면 이미지의 버전이 흔들린다.
# 버전은 airflow/Dockerfile.local의 AIRFLOW_VERSION, 즉 실행 컨테이너와 맞춘다.
#
# provider 버전 충돌을 막기 위해 반드시 공식 constraints와 함께 설치할 것
# (설치 명령은 .github/workflows/ci.yml 참고).
apache-airflow==3.3.0
# DAG 파싱에 필요한 provider만 넣는다 (airflow/dags 와 그 DAG들이 import하는
# jobs 에서 실제로 쓰는 것들). 늘어나면 여기에 추가한다.
apache-airflow-providers-standard
apache-airflow-providers-postgres
